# Current Time Tool

Registers `get_current_time`, which fetches an HTTPS Date header, updates the system clock, and returns the local time.

Permissions: `network`.

Configuration: none.

Known limits: requires network reachability to the configured time source.
