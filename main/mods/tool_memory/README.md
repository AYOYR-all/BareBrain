# Memory Tools

Registers the indexed-memory tool set used by the agent to search, read, expand, delete, queue, and inspect memory work.

Permissions: `storage.read`, `storage.write`.

Configuration: memory paths and model settings are provided by the memory subsystem.

Known limits: note indexing is asynchronous, so newly queued content is not immediately searchable.
